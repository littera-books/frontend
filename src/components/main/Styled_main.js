import styled from 'styled-components';

const SliderWrapper = styled.div`
  display: block;
`;

const CardWrapper = styled.div`
  margin: 1rem;
  max-width: 13rem;
`;

export default {
  SliderWrapper,
  CardWrapper,
};
