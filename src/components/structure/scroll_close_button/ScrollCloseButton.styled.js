import styled from 'styled-components';
import Wrapper from '../../../styled_base/Wrapper';

const ScrollCloseButtonWrapper = styled(Wrapper.BasicBlockWrapper)`
  position: sticky;
  top: 5rem;
  left: 0;
  margin-left: 43.25rem;
`;

export default {
  ScrollCloseButtonWrapper,
};
