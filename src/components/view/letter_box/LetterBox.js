import React from 'react';

// Styled
import Wrapper from '../../../styled_base/Wrapper';

const LetterBox = () => (
  <Wrapper.FlexWrapper>
    <p>letter box</p>
  </Wrapper.FlexWrapper>
);

export default LetterBox;
