import React from 'react';

// Styled
import Wrapper from '../../../styled_base/Wrapper';

const SendLetter = () => (
  <Wrapper.FlexWrapper>
    <p>letter</p>
  </Wrapper.FlexWrapper>
);

export default SendLetter;
