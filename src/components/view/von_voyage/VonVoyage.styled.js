import styled from 'styled-components';

const SliderWrapper = styled.div`
  position: relative;
  display: block;
`;

const ProductItem = styled.div`
  text-align: center;
  margin: 0 4rem;
`;

export default {
  SliderWrapper,
  ProductItem,
};
