import React from 'react';

// Styled
import Wrapper from '../../../styled_base/Wrapper';

const Accept = () => (
  <Wrapper.FlexWrapper>
    <p>accept</p>
  </Wrapper.FlexWrapper>
);

export default Accept;
